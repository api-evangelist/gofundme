{
  "opencollection": "1.0.0",
  "info": {
    "name": "GoFundMe Pro Magic Link API",
    "version": "2.0.0"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "client_credentials",
      "accessTokenUrl": "/oauth2/auth",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "MagicLink",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Create an async, mint-only batch of scoped magic links (internal).",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://pro.gofundme.com/api/2.0/scoped-magic-link/batch",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Create a new magic-link batch (async, mint-only).\n\nDispatches synchronously so the controller can return the batch_id and\nacceptance summary immediately (202 Accepted); the actual link-minting is\nhandled by the async orchestrator dispatched inside CreateMagicLinkBatch.\n\nApp-token-only: privateEndpoint blocks external apps, and\nassertAppTokenOnly() additionally rejects internal tokens that carry\nmember context. A batch can span multiple orgs and is triggered\nservice-to-service (the Admin bulk-gen"
        },
        {
          "info": {
            "name": "Return the rollup status for a magic-link batch (internal).",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://pro.gofundme.com/api/2.0/scoped-magic-link/batch/:batch",
            "params": [
              {
                "name": "batch",
                "value": "",
                "type": "path"
              }
            ]
          },
          "docs": "Return the rollup status for a batch.\n\nUnscoped findOrFail is intentional: privateEndpoint + app-token-only gate\nthe route and the id is the batch's own surrogate key (not a tenant\nresource), so there is no cross-org IDOR surface."
        }
      ]
    }
  ],
  "bundled": true
}